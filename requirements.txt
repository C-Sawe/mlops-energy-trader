# Sprint 1 - DataOps foundation
pandas>=2.2
numpy>=1.26
sqlalchemy>=2.0
psycopg2-binary>=2.9
yfinance>=0.2.40
python-dotenv>=1.0

# Sprint 2 - RLOps environment, metrics, baselines
gymnasium>=0.29
stable-baselines3>=2.3
scipy>=1.11

# Sprint 3 - RLOps training
mlflow>=2.17
# torch is pulled in transitively by stable-baselines3 already

# finrl is deliberately NOT here: it was used only for the one-off §8
# cross-check validation experiment (scripts/finrl_crosscheck.py), installed
# into an isolated venv to avoid its dated dependency footprint conflicting
# with the versions pinned above. See that script's docstring.

# Sprint 4 - Serving & orchestration
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27  # TestClient for the FastAPI test suite, no live server needed

# Development
pytest>=8.0
